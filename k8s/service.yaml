apiVersion: v1
kind: Service
metadata:
  name: claude-agent-server
  namespace: claude-agent
spec:
  selector:
    app: claude-agent-server
  ports:
    - name: http
      port: 8443
      targetPort: http
      protocol: TCP
  type: ClusterIP
